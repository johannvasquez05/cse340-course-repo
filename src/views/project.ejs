<%- include('partials/header') %>

    <main>
        <h1>
            <%= project.title %>
        </h1>

        <div>
            <p><strong>Date:</strong>
                <%= new Date(project.date).toLocaleDateString('en-US', { year: 'numeric' , month: 'long' ,
                    day: 'numeric' }) %>
            </p>
            <p><strong>Location:</strong>
                <%= project.location %>
            </p>
            <p><strong>Partner Organization:</strong> <a href="/organization/<%= project.organization_id %>">
                    <%= project.organization_name %>
                </a></p>

            <p><strong>Categories:</strong>
                <% if (categories && categories.length> 0) { %>
                    <% categories.forEach((category, index)=> { %>
                        <a href="/category/<%= category.category_id %>">
                            <%= category.name %>
                        </a>
                        <%= index < categories.length - 1 ? ', ' : '' %>
                            <% }); %>
                                <% } else { %>
                                    None
                                    <% } %>
                                        <p><a href="/assign-categories/<%= project.project_id %>">Edit Categories</a>
                                        </p>
            </p>

            <h2>About this Project</h2>
            <p>
                <%= project.description %>
            </p>
        </div>

        <p><a href="/edit-project/<%= project.project_id %>">Edit Project Details</a></p>
    </main>

    <%- include('partials/footer') %>